import { SafeAreaView } from 'react-native';
import { HomeView } from './app/views/HomeView/HomeView';

export const App = () => {
  return (
    <SafeAreaView>
      <HomeView />
    </SafeAreaView>
  );
};
